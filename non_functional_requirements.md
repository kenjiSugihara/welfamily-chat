# Welfamily アプリケーション 非機能要件（NFR）サマリー

現在の仕様（`prompt.txt`）および Streamlit を用いた構成に基づく、非機能要件（Non-Functional Requirements）を整理しました。
アプリの品質や運用方針を決める上で重要な指針となります。

## 1. 性能・パフォーマンス (Performance)
- **応答速度**: Streamlitはユーザーが操作（ボタン押下など）するたびにスクリプト全体を再実行する仕様ですが、今回は重いデータ処理や外部API通信がないため、数ミリ秒〜1秒以内で瞬時に結果が表示される設計となっています。
- **軽量性**: 画像リソースなどの重いアセットを含まないため、初期ロードも高速です。

## 2. ユーザビリティ・アクセシビリティ (Usability)
- **レスポンシブ対応**: Streamlitの標準仕様により、PCだけでなくスマートフォンやタブレットでの表示にも自動対応します。（※スマートフォンの縦画面では、男性・女性の2カラムが自動的に縦に並んで表示されます）。
- **ブランド視認性**: 指定されたブランドカラー（モスグリーン、アースゴールド）と背景色（薄いグレーグリーン）を活用し、目に優しく落ち着いたUIを提供します。

## 3. セキュリティとプライバシー (Security & Privacy)
- **データの非保存（ステートレス）**: 現在の仕様では、ユーザーが入力した回答データや診断結果はサーバーやデータベースに一切保存されません。ブラウザを閉じるとデータは完全に消去されます。
- **個人情報保護リスクの極小化**: ユーザー登録（ログイン）機能を持たず、個人情報（PII）を取り扱わないため、現段階での情報漏洩リスクはほぼありません。

## 4. 可用性と運用 (Availability & Hosting)
- **稼働環境 (Streamlit Community Cloud想定)**: 
  - 無料枠で公開した場合、アクセスが一定期間（約7日間）ないとアプリが「スリープ状態」になります。
  - スリープ状態から復帰する際、最初のユーザーは画面表示までに数十秒〜1分程度の待機時間が発生します。
- **商用化に向けた課題**: 将来的に多数のユーザーが同時に利用するサービスへ成長させる場合は、より可用性の高い専用サーバー（AWS, GCP, Herokuなど）への移行を検討する必要があります。

## 5. 保守性と拡張性 (Maintainability)
- **テキストデータの分離**: 16パターンの診断結果テキストは、スコアリングの計算ロジックとは切り離して管理（データ辞書化）しているため、文言の修正や追加がエンジニア以外でも容易に行える構造になっています。
- **機能拡張**: 今後「診断結果をPDFでダウンロードする」「LINEに結果をシェアする」といった機能を追加しやすいように、Pythonエコシステム（Streamlit）に依存したシンプルな単一ファイル（`app.py`）構成を採用しています。
