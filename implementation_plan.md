# Welfamily アプリケーション実装計画

仕様書（`prompt.txt`）に基づき、Welfamily（夫婦・パートナー関係改善サービス）の機能要件と開発プラン（Task plan）をまとめました。

## 1. 機能要件サマリー

### 画面・UI要件
- **フレームワーク**: Streamlit (Python)
- **レイアウト**: 全体は2カラム構成（左: 男性、右: 女性）。上部に案内テキストを配置。
- **入力方式**: 男女各5問、1〜5の5段階評価スライダー（`st.slider`、初期値3）。
- **デザイン (カスタムCSS)**:
  - 全体の背景色: 薄いグレーグリーン (`#f9faf9`)
  - メインカラー: モスグリーン (`#4a5d4e`) - ボタン背景、結果カードの上部ライン、男性用Tryボックスの境界線、タイトルテキスト。
  - アクセントカラー: アースゴールド (`#c5a059`) - タイプ名のアンダーライン、女性用Tryボックスの境界線。
  - コンポーネント: 診断ボタンは角丸(8px)、ホバー時の色変化と浮き上がりアニメーション。結果カードには薄いシャドウを付与し、落ち着きと信頼感を演出。

### ロジック要件
- **スコアリング**: 各設問（m1~m5, f1~f5）の値に対して、仕様書で指定された重み付け数式を計算し、男性4タイプ（A, B, C, D）、女性4タイプ（X, Y, Z, W）それぞれの合計スコアを算出。
- **判定**: 男女それぞれで最もスコアが高かったタイプを判定し、4×4＝16通りの組み合わせパターンを特定する。

### 出力要件
- 16パターンの組み合わせごとに用意された「タイトル」「解説」「男Try」「女Try」のテキストを、一切省略せずに画面上に表示する。

---

## User Review Required

> [!IMPORTANT]
> スコアリングにおいて、**合計スコアが同点になった場合の処理**について仕様書に明記されていません。
> 今回の実装では、プログラムの標準的な挙動（同点の場合はリスト内で先に計算されたものを優先＝AやXを優先）とする想定ですが、よろしいでしょうか？（もし「同点の場合は特定のタイプを優先する」などのルールがあればお知らせください。）

## Proposed Changes

### Streamlit Application
#### [NEW] [app.py](file:///c:/Users/sugih/OneDrive/Desktop/業務/2.Welfamily/welfamily-chat/app.py)
- カスタムCSSの定義と注入 (`st.markdown(..., unsafe_allow_html=True)`)
- 2カラムレイアウトと各10個のスライダーUI実装
- 判定用スコア計算関数（男性用・女性用）の実装
- 16パターンの診断結果データ（辞書型等）と表示用UIの実装

#### [NEW] [requirements.txt](file:///c:/Users/sugih/OneDrive/Desktop/業務/2.Welfamily/welfamily-chat/requirements.txt)
- `streamlit`

## Verification Plan

### Automated Tests
- 実装完了後、ターミナルで自律的に `streamlit run app.py` を実行し、起動時にSyntax Error等の例外が発生しないか検証します。エラーがあれば自己修正して完成させます。

### Manual Verification
- 実装完了後、ユーザー様にて実際にブラウザ上でスライダーを動かし、以下の点をご確認いただきます。
  - スコアリングによる診断結果の切り替わりが正しいか
  - ホバーアニメーションや指定色がブランドガイドライン通りに反映されているか
